% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cd_stac_catalog.R
\name{cd_stac_catalog}
\alias{cd_stac_catalog}
\title{Generate a static STAC catalog from COGs}
\usage{
cd_stac_catalog(
  cog_dir,
  output_path = "catalog.json",
  catalog_id = "era5-land",
  title = "ERA5-Land Climate Data",
  description = NULL,
  base_url = "https://stac-era5-land.s3.us-west-2.amazonaws.com"
)
}
\arguments{
\item{cog_dir}{Character. Directory containing COG files (.tif).}

\item{output_path}{Character. Path to write the catalog JSON.
Default \code{"catalog.json"}.}

\item{catalog_id}{Character. STAC catalog ID.
Default \code{"era5-land"}.}

\item{title}{Character. Human-readable catalog title.}

\item{description}{Character. Optional catalog description.}

\item{base_url}{Character. Base URL where COGs will be served.
Asset hrefs are built as \code{{base_url}/{filename}}.}
}
\value{
The output path (invisibly).
}
\description{
Scans a directory for Cloud-Optimized GeoTIFFs and builds a
STAC catalog JSON file. Each COG becomes one item with
\code{cd:variable} and \code{cd:period} properties parsed from the filename.
The resulting catalog is compatible with \code{\link[=cd_catalog]{cd_catalog()}}.
}
\examples{
\dontrun{
cd_stac_catalog(
  "data/cogs",
  output_path = "data/catalog.json",
  base_url = "https://stac-era5-land.s3.us-west-2.amazonaws.com"
)
}

}
