% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cd_trend.R
\name{cd_trend}
\alias{cd_trend}
\title{Compute trend statistics}
\usage{
cd_trend(x, trend_start = c(1950, 1980))
}
\arguments{
\item{x}{A tibble from \code{\link[=cd_extract]{cd_extract()}} or \code{\link[=cd_anomaly]{cd_anomaly()}} with columns
\code{variable}, \code{period}, \code{year}, and either \code{value} or \code{anomaly}.}

\item{trend_start}{Integer vector of start years for trend windows.
Default \code{c(1950, 1980)}.}
}
\value{
A tibble with columns \code{variable}, \code{period}, \code{trend_start},
\code{slope}, \code{intercept}, \code{mk_pvalue}, \code{n_years}.
}
\description{
Runs Mann-Kendall significance test and Theil-Sen slope estimator
on time series data for each variable, period, and trend start year.
}
\examples{
catalog <- cd_catalog(
  system.file("extdata", "example_catalog.json", package = "cd")
)
aoi <- sf::st_read(
  system.file("extdata", "example_aoi.gpkg", package = "cd"),
  quiet = TRUE
)
ts <- cd_extract(catalog, aoi)
cd_trend(ts, trend_start = 1951)

}
