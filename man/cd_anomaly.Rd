% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cd_anomaly.R
\name{cd_anomaly}
\alias{cd_anomaly}
\title{Compute climate anomalies}
\usage{
cd_anomaly(x, baseline, cap_pct = 200)
}
\arguments{
\item{x}{A tibble from \code{\link[=cd_extract]{cd_extract()}} with columns \code{variable},
\code{period}, \code{year}, \code{value}.}

\item{baseline}{A tibble from \code{\link[=cd_baseline]{cd_baseline()}} with columns
\code{variable}, \code{period}, \code{baseline_mean}.}

\item{cap_pct}{Numeric. Cap for percent-of-normal anomalies.
Values beyond +/- \code{cap_pct} are clamped. Default \code{200}.}
}
\value{
A tibble with columns \code{variable}, \code{period}, \code{year},
\code{anomaly}, \code{anomaly_type}, \code{unit}.
}
\description{
Calculates departure from a baseline for each year. Uses
\code{\link[=cd_variables]{cd_variables()}} to determine the anomaly type: absolute deviation
for temperature, VPD, and RH; percent of normal for precipitation
and soil moisture.
}
\examples{
catalog <- cd_catalog(
  system.file("extdata", "example_catalog.json", package = "cd")
)
aoi <- sf::st_read(
  system.file("extdata", "example_aoi.gpkg", package = "cd"),
  quiet = TRUE
)
ts <- cd_extract(catalog, aoi)
bl <- cd_baseline(ts, baseline_years = 1951:1955)
cd_anomaly(ts, bl)

}
