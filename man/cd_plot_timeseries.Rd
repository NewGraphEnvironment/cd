% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cd_plot_timeseries.R
\name{cd_plot_timeseries}
\alias{cd_plot_timeseries}
\title{Plot climate anomaly time series}
\usage{
cd_plot_timeseries(
  x,
  variable = NULL,
  period = "annual",
  trend = NULL,
  title = NULL,
  colors = c(pos = "#d73027", neg = "#4575b4")
)
}
\arguments{
\item{x}{A tibble from \code{\link[=cd_anomaly]{cd_anomaly()}} with columns \code{variable},
\code{period}, \code{year}, \code{anomaly}. Also works with \code{\link[=cd_extract]{cd_extract()}} output
(uses \code{value} column).}

\item{variable}{Character. Which variable to plot. Default uses
the first variable in \code{x}.}

\item{period}{Character. Which period to plot. Default \code{"annual"}.}

\item{trend}{Optional tibble from \code{\link[=cd_trend]{cd_trend()}} to overlay trend lines.}

\item{title}{Optional plot title.}

\item{colors}{Named character vector of length 2 for positive/negative
bar colors. Default \code{c(pos = "#d73027", neg = "#4575b4")}.}
}
\value{
A \link[ggplot2:ggplot]{ggplot2::ggplot} object.
}
\description{
Creates a bar chart of anomalies over time with optional Theil-Sen
trend lines. Positive and negative anomalies are colored differently.
}
\examples{
\dontrun{
catalog <- cd_catalog()
aoi <- sf::st_read("my_aoi.gpkg")
ts <- cd_extract(catalog, aoi, variables = "tmean", periods = "annual")
bl <- cd_baseline(ts, baseline_years = 1951:1980)
ano <- cd_anomaly(ts, bl)
trn <- cd_trend(ano, trend_start = c(1951, 1981))
cd_plot_timeseries(ano, trend = trn)
}

}
