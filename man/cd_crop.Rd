% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cd_crop.R
\name{cd_crop}
\alias{cd_crop}
\title{Crop and mask a raster to an AOI}
\usage{
cd_crop(href, aoi)
}
\arguments{
\item{href}{Character. Path or URL to a COG or raster file.}

\item{aoi}{An \code{sf} or \code{SpatVector} polygon to crop to.}
}
\value{
A \link[terra:SpatRaster-class]{terra::SpatRaster} cropped and masked to the AOI.
}
\description{
Reads a COG (local or remote) and crops it to a user-supplied area
of interest polygon. Works with local file paths and remote URLs
via GDAL's \verb{/vsicurl/}.
}
\examples{
href <- system.file("extdata", "example_climate.tif", package = "cd")
aoi <- sf::st_read(
  system.file("extdata", "example_aoi.gpkg", package = "cd"),
  quiet = TRUE
)
r <- cd_crop(href, aoi)
r

}
