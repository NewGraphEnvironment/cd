% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cd_summary.R
\name{cd_summary}
\alias{cd_summary}
\title{Format trend results as a reporting table}
\usage{
cd_summary(trend, region_name = NULL)
}
\arguments{
\item{trend}{A tibble from \code{\link[=cd_trend]{cd_trend()}}.}

\item{region_name}{Optional character label for the AOI. If provided,
adds a \code{Region} column.}
}
\value{
A tibble with columns \code{Parameter}, \code{Period}, \code{Slope}, \code{Years},
\verb{Total Change}, \code{Unit}, \code{p-value}, and optionally \code{Region}.
}
\description{
Joins trend statistics with variable metadata from \code{\link[=cd_variables]{cd_variables()}}
and computes Total Change (slope x years). Returns a tibble ready
for \code{\link[DT:datatable]{DT::datatable()}} or \code{\link[gt:gt]{gt::gt()}}.
}
\examples{
catalog <- cd_catalog(
  system.file("extdata", "example_catalog.json", package = "cd")
)
aoi <- sf::st_read(
  system.file("extdata", "example_aoi.gpkg", package = "cd"),
  quiet = TRUE
)
ts <- cd_extract(catalog, aoi)
trn <- cd_trend(ts, trend_start = 1951)
cd_summary(trn)

}
