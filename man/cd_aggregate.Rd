% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cd_aggregate.R
\name{cd_aggregate}
\alias{cd_aggregate}
\title{Aggregate monthly rasters to seasonal and annual periods}
\usage{
cd_aggregate(x, method = "mean", seasons = cd_seasons())
}
\arguments{
\item{x}{A \link[terra:SpatRaster-class]{terra::SpatRaster} with 12 bands (one per month,
Jan through Dec).}

\item{method}{Character. Aggregation method: \code{"mean"} or \code{"sum"}.
Default \code{"mean"}. Use \code{"sum"} for precipitation.}

\item{seasons}{Named list of integer vectors defining month groups.
Default uses standard meteorological seasons:
winter (DJF), spring (MAM), summer (JJA), fall (SON).}
}
\value{
A named list of \link[terra:SpatRaster-class]{terra::SpatRaster} objects, one per period.
Names are \code{"annual"}, plus the names from \code{seasons}.
}
\description{
Takes a multi-band raster with one band per month and aggregates
to annual and seasonal periods. Season definitions are configurable.
}
\examples{
\dontrun{
# 12-band raster (one per month)
r <- terra::rast(nrows = 10, ncols = 10, nlyrs = 12)
terra::values(r) <- matrix(rnorm(1200), 100, 12)

# Default seasons
periods <- cd_aggregate(r)
names(periods)  # "annual", "winter", "spring", "summer", "fall"

# Custom seasons (e.g., wet/dry)
cd_aggregate(r, seasons = list(wet = 10:3, dry = 4:9))
}

}
